# Application Name
spring.application.name=sns-clone

# Profile Name
spring.profiles.active=local-s3

# DataSource Configuration
spring.datasource.url=jdbc:h2:tcp://localhost/~/testDB
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA Configuration
spring.jpa.hibernate.ddl-auto=create
#spring.jpa.show_sql=true

# Session Management
#spring.session.store-type=jdbc
#spring.session.jdbc.initialize-schema=always

# Profile Configuration
spring.profiles.include=oauth,email

# Session Timeout Configuration for Testing
#server.servlet.session.timeout=30s

# Url
cors.allowed-origins=http://localhost:3000
app.reset-password.url=http://localhost:3000/reset-password?token=

# Spring Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=PigeonStargram@gmail.com
spring.mail.password=qiqfbsbrkvknqeon
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# S3 bucket
aws.accessKeyId=AKIA4RCAONJYGOQ5Q2EI
aws.secretAccessKey=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
aws.region=ap-northeast-2
aws.s3.bucketName=pigeonstargram-image

# Session Management
spring.session.store-type=redis
spring.session.redis.flush-mode=immediate
spring.session.redis.namespace=spring:session
spring.session.timeout=1800s

# Redis Configuration
spring.data.redis.host=localhost
spring.data.redis.port=6379
# spring.redis.password= #
# spring.data.redis.database=0