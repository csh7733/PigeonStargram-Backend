package com.pigeon_stargram.sns_clone.worker.notification;

import com.pigeon_stargram.sns_clone.worker.Worker;

/**
 * 알림 작업을 처리하는 워커 인터페이스입니다.
 *
 * 이 인터페이스는 Worker 인터페이스를 확장하여 알림 작업을 처리하는 워커의
 * 공통 동작을 정의합니다.
 */
public interface NotificationWorker extends Worker {
}
